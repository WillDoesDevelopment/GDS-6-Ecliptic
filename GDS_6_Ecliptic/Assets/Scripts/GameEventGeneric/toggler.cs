using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class toggler : MonoBehaviour
{
    /*public tester tester1;
    public tester tester2;
    public GameEventListener listener;

    private bool toggle;

    private void Start()
    {
        toggle = true;

        ChangeListener();
    }

    public void Toggle()
    {
        listener.onEventTriggered.RemoveAllListeners();

        toggle = !toggle;

        ChangeListener();

        Debug.Log(toggle);
    }

    private void ChangeListener()
    {
        if (toggle)
            listener.onEventTriggered.AddListener(tester1.Tester);
        else
            listener.onEventTriggered.AddListener(tester2.Tester);
    }*/
}
